name: Update Article AI

on:
  push:
    branches:
      - "add-blog-article-chat"
    # paths:
    #   - "client/constants/blogs"

jobs:
  update-article-vector-store:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
