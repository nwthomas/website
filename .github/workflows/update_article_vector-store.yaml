name: Update Article AI

on:
  push:
    branches:
      - "add-blog-article-chat"
    # paths:
    #   - "client/constants/blogs"

jobs:
  update-article-vector-store:
    name: "Update Article Vector Store"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.12"
